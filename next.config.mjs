/** @type {import('next').NextConfig} */
const nextConfig = {
    images : {
        domains : ['images.unsplash.com' , 'assets.aceternity.com' , 'cdn-icons-png.flaticon.com']
     }
};

export default nextConfig;
